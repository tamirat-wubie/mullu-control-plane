# Sandbox-to-Live Promotion Path

Purpose: define the reusable promotion read model that prevents capabilities
from jumping from local proof directly to live action.
Governance scope: [OCE, RAG, CDCV, CQTE, UWMA, SRCA, PRS]
Dependencies: capability passports, gate template registry, evidence passports,
promotion schema, and promotion validator.
Invariants: every capability has one ordered promotion path; live execution is
disabled in Foundation Mode; pilot, limited-live, approved-live, and production
stages remain blocked until evidence and approval exist.

## Architecture

The canonical path is:

```text
sandbox
-> local_demo
-> dry_run
-> operator_review
-> pilot
-> limited_live
-> approved_live
-> production
```

The read model is generated by:

```text
mcoi/mcoi_runtime/app/sandbox_to_live_promotion.py
```

It consumes:

1. capability passports
2. gate template registry
3. evidence passports

It emits:

1. `examples/sandbox_to_live_promotion.foundation.json`
2. one ordered promotion path per capability
3. stage state for sandbox through production
4. missing controls per blocked or current stage
5. summary counters by current stage and family

## Stage Semantics

| Stage | Meaning |
| --- | --- |
| `sandbox` | Capability is registered and bounded for isolated proof. |
| `local_demo` | Capability has local demo or mock-evaluation evidence. |
| `dry_run` | Capability has no-effect rehearsal, receipt, and replay obligations. |
| `operator_review` | Capability needs approval, evidence review, or recovery review. |
| `pilot` | Capability is eligible only for bounded pilot planning after live evidence. |
| `limited_live` | Capability requires bounded live receipts and active rollback coverage. |
| `approved_live` | Capability requires approved live action evidence and terminal receipts. |
| `production` | Capability requires production witness evidence and retained monitoring. |

## Foundation Boundary

These fields must remain fixed:

```text
promotion_path_set_is_not_execution_authority = true
live_execution_enabled = false
promotion_path_is_not_execution_authority = true
live_action_enabled = false
stage.live_execution_allowed = false
```

The promotion path is visibility and planning only. Execution authority remains
with governed capability packs, UAO, gate templates, approval policy, evidence
verification, receipts, and rollback policy.

## Validation

Run:

```powershell
python scripts/validate_sandbox_to_live_promotion.py
python -m pytest tests/test_validate_sandbox_to_live_promotion.py -q
python scripts/validate_schemas.py
```

Focused workspace governance:

```powershell
python scripts/run_workspace_governance_checks.py --check agents_policy --check strict_schema_validation --json
```

## Change Deltas

Constructive deltas:

1. Adds a deterministic sandbox-to-live promotion path projection.
2. Binds every capability to the same eight-stage promotion sequence.
3. Keeps live stages blocked in Foundation Mode.
4. Adds strict schema validation and pytest coverage.

Fracture deltas: none. This is additive and does not alter execution authority.

## Status

Outcome: AwaitingEvidence until validators pass in the active workspace.

Next action: add Capability Debt Report after promotion path validation is
green.
