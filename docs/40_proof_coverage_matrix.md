# Proof Coverage Matrix

Purpose: define the current request-proof, action-proof, runtime-witness, and
audit-chain coverage for externally callable MCOI and gateway surfaces.

Governance scope: this document summarizes the canonical matrix generated by
`scripts/proof_coverage_matrix.py`. The JSON fixture is the machine witness; this
document is the operator-readable witness.

| Surface | Representative paths | Request proof | Action proof | Runtime witnesses | Audit | Status |
|---|---|---|---|---|---|---|
| `gateway_capability_fabric` | `/webhook/*`, `/capability-fabric/read-model`, `/commands/{command_id}/closure` | yes | yes | closure invariants | hash-chain | Command admission, request receipt envelopes, and terminal closure expose runtime witnesses. |
| `llm_streaming` | `/api/v1/stream`, `/api/v1/chat/stream` | yes | yes | none | hash-chain | Streaming emits precharge, first-byte, chunk-debit, and final-reconcile proof identifiers. |
| `tool_invocation` | `/api/v1/tools/invoke`, `/api/v1/workflow/tools` | yes | yes | policy receipts | hash-chain | Tool invocation routes bind action proof ids to capability policy receipts over argument hashes. |
| `governed_session` | `GovernedSession.llm`, `GovernedSession.execute`, `GovernedSession.query` | yes | yes | request envelopes | hash-chain | Session entry points return request-envelope proofs and retain action proof lineage. |

Gateway runtime witness invariants:

1. `command_lifecycle_events_are_hash_linked`
2. `terminal_closure_requires_evidence_refs`
3. `successful_response_is_bound_to_response_evidence_closure`

Resolved closure actions:

1. `bind_tool_arguments_to_capability_policy_receipts`
2. `normalize_gateway_request_receipt_envelopes`

STATUS:
  Completeness: 100%
  Invariants verified: route declarations, coverage levels, closure action mapping, gateway runtime witness mapping, tool policy receipt mapping, governed session request envelope mapping, gateway request receipt normalization
  Open issues: none
  Next action: run `python scripts/proof_coverage_matrix.py --check`
