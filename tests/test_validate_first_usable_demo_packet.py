"""Transitional path for the first usable demo packet tests.

The active CI-gated assertions live in
``tests/test_validate_foundation_first_usable_demo_packet_boundary.py`` so they
match the existing Foundation validator workflow glob. This file is retained as
a non-executing compatibility note until the branch can safely remove the old
path in a local git operation.
"""
