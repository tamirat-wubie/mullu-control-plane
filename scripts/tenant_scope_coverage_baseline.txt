# Tenant-Scope Coverage Baseline
#
# Handlers that take a tenant from the path/body but do NOT yet call a
# tenant-scope helper (enforce_tenant_scope / scoped_listing_tenant). Recorded
# when the validate_tenant_scope_coverage.py gate was introduced, so the gate
# blocks NEW unscoped tenant routes while this inherited set is retired
# incrementally. Each line is "relpath::handler".
#
# These were NOT introduced here — the linter surfaced them; the prior manual
# audit only reached tenant.py / api_keys / audit / conversations / governance /
# connectors. Prioritize the cross-tenant *data read/export* handlers (compliance
# exports, llm/costs, agent memory, workflow) over operator/bootstrap routes
# (organization_kernel, pilot, rbac.create_identity, tenant.register/budget)
# whose intended access model (operator-only vs tenant-self) should be confirmed
# before scoping.
#
# Confirmed not-tenant-scoped: compile/preview endpoints that echo the request
# (tenant_id labels the returned plan; no stored-tenant-state read and no side
# effect), so there is no cross-tenant data to scope.
mcoi/mcoi_runtime/app/routers/assistant.py::compile_finance_ops_assistant_plan
mcoi/mcoi_runtime/app/routers/explain.py::explain_action
#
# Newly surfaced by the scalar `tenant_id` query/path-parameter check (added to
# the linter alongside the path/body checks). These take a caller-chosen
# `tenant_id` query parameter and read stored state by it, so the linter now
# tracks them. Scoping is deferred ONLY pending per-handler access-model review
# (tenant-self listing -> scoped_listing_tenant, vs operator console ->
# require_admin); none is a confirmed echo. Drain incrementally. The verified
# high-severity financial reads (workflow.get_ledger, finance approval packet
# list/read-model/by-id) and the session-creation write were scoped in the same
# change that introduced this check, so they are intentionally absent here.
mcoi/mcoi_runtime/app/routers/agent.py::list_webhooks
mcoi/mcoi_runtime/app/routers/console.py::console_audit
mcoi/mcoi_runtime/app/routers/console.py::console_runs
mcoi/mcoi_runtime/app/routers/god_mode.py::list_tickets
mcoi/mcoi_runtime/app/routers/ops/feature_flags.py::check_flag
mcoi/mcoi_runtime/app/routers/pilot.py::list_pilot_provisions
mcoi/mcoi_runtime/app/routers/rbac.py::list_identities
mcoi/mcoi_runtime/app/routers/temporal_scheduler.py::list_temporal_schedules
